nav.navbar.navbar-default.navbar-fixed-top(role='navigation')
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') Home
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li(class=isActive('/articles/new') ? 'active' : '')
          a(href="/articles/new", title="new article") 新建
        - if (req.isAuthenticated())
          li
            a(href="/users/"+req.user.id, title="Profile") 简介
          li
            a(href="/logout", title="logout") 登出
        - else
          li(class=isActive('/login') ? 'active' : '')
            a(href="/login", title="Login") 登录
